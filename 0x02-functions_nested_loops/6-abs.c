#include "main.h"

/**
 * 6-abs.c -computes the absolute value of an integer
 * @i: input number as an integer
 * Return: absolute value
 */
int_abs(int i);
{
	if (i >= 0)
	{
		return (i);
	}
		return (-i);
}
